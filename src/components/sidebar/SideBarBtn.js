import React from 'react'

const SideBarBtn = ({ icon }) => {
  return (
    <div>
        <button 
            className="sidebar-icon">
            {icon}
    </button>
    </div>
  )
}

export default SideBarBtn